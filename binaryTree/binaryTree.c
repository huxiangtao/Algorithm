#include<stdio.h>
void insert(binaryItem * , int);
typedef strcut binaryItem {
	binaryItem * left;
	binaryItem * right;
	int value;	
} NODE;

int main(void) {
	return 0;
}

void insert(NODE * node,int val) {
	if(node != NULL) {
		if(node.value > val)
			node.left = &NODE;
		else
			node
	}
}
